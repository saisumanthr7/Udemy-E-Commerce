let simplifiedArray: number[] = [1,2,3,4];

for(let i=0; i < simplifiedArray.length; i++){
    console.log(simplifiedArray[i]);
}


// simplified for loop

for(let printNums of simplifiedArray){
    console.log(printNums);
}